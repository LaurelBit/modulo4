<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title><%= producto.id ? 'Editar Producto' : 'Nuevo Producto' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h2><%= producto.id ? 'Editar Producto' : 'Nuevo Producto' %></h2>
        <form action="<%= producto.id ? '/productos/actualizar/' + producto.id : '/productos/guardar' %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label>Nombre:</label>
                <input type="text" name="nombre" class="form-control" value="<%= producto.nombre || '' %>" required>
            </div>
            <div class="mb-3">
                <label>Precio:</label>
                <input type="number" step="0.01" name="precio" class="form-control" value="<%= producto.precio || '' %>" required>
            </div>
            <div class="mb-3">
                <label>Imagen:</label>
                <input type="file" name="imagen" class="form-control">
                <% if (producto.imagen) { %>
                    <img src="/images/<%= producto.imagen %>" width="100" class="mt-2">
                    <input type="hidden" name="imagen_actual" value="<%= producto.imagen %>">
                <% } %>
            </div>
            <div class="mb-3">
                <label>Detalle:</label>
                <textarea name="detalle" class="form-control" required><%= producto.detalle || '' %></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Guardar</button>
            <a href="/productos" class="btn btn-secondary">Volver</a>
        </form>
    </div>
</body>
</html>
